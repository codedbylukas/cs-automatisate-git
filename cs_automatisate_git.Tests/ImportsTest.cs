using CommandRunner;
using BranchMode;
using Commits;
using initConfig;
using cs_automatisate_git;
using Push;
using CommandRunner;

public class ImportsTest
{
    [Fact]
    public void TestImports()
    {
        // This test will fail if any of the imports are incorrect or if there are missing dependencies.
        Assert.True(true);
    }
}
